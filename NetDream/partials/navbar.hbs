<header class="navbar">
    <div class="navbar__logo">
        <a id="logo" href="{{@website.url}}">
            {{#if @website.logo}}
            <img src="{{@website.logo}}" alt="{{@website.name}}">
            {{else}}
            {{{@config.custom.headerLogoText}}}
            {{/if}}
        </a>
    </div>

    {{#if menus.mainMenu}}
    {{> menu menus.mainMenu}}
    {{/if}}

    {{{@customSearchInput}}}
</header>
